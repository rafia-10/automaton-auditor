# ──────────────────────────────────────────────────────────────────────────────
# Automaton Auditor — required environment variables
# Copy this file to .env and fill in your secrets:
#   cp .env.example .env
#
# ⚠️  NEVER commit your real .env file — it is already in .gitignore.
# ──────────────────────────────────────────────────────────────────────────────

# ── OpenAI (required if you use LLM-based nodes / embeddings) ────────────────
OPENAI_API_KEY=sk-...

# ── LangSmith tracing (optional but recommended for debugging the graph) ──────
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=ls__...
LANGCHAIN_PROJECT=automaton-auditor

# ── GitHub (optional — increases API rate limits for public repo cloning) ─────
# Generate a Personal Access Token with read-only repo scope:
# https://github.com/settings/tokens/new?scopes=repo
GITHUB_TOKEN=ghp_...

# ── Logging ───────────────────────────────────────────────────────────────────
# Set to DEBUG for verbose output, INFO for normal operation
LOG_LEVEL=INFO
